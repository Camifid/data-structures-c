#include <stdio.h>

int main() {
	char letra;
	
	printf("\nDigite uma letra: ");
	scanf("%c", &letra);
	printf("\nO valor da variável 'letra' é: %c. \n", letra);
	
  return 0;
}

/* 
	float num, soma;
	printf("\nDigite um número: ");
	scanf("%f", &num);
	soma = num + 5;
	
	printf("\nO valor da variável 'num' + 5 é: %.1f\n\n", soma);
*/ 
